# This file is generated by `ray project create`.

# A unique identifier for the head node and workers of this cluster.
cluster_name: anyscale-academy

# The maximum number of workers nodes to launch in addition to the head
# node. This takes precedence over min_workers. min_workers defaults to 0.
max_workers: 0

# Cloud-provider specific configuration.
provider:
    type: aws
    region: us-west-2
    availability_zone: us-west-2a

# Provider-specific config for the head node, e.g. instance type. By default
# Ray will auto-configure unspecified fields such as SubnetId and KeyName.
# For more documentation on available fields, see:
# http://boto3.readthedocs.io/en/latest/reference/services/ec2.html#EC2.ServiceResource.create_instances
head_node:
    InstanceType: m5.xlarge
    ImageId: ami-0e8271975f6af5c8d # 20.04 on 2020-07-01

    # You can provision additional disk space with a conf as follows
    BlockDeviceMappings:
        - DeviceName: /dev/sda1
          Ebs:
              VolumeSize: 100


# How Ray will authenticate with newly launched nodes.
auth:
    ssh_user: ubuntu

# List of shell commands to run to set up nodes.
setup_commands:
    # Setup Anaconda.
    - curl -sLo miniconda.sh https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
    - bash miniconda.sh -b -u -p $HOME/anaconda3
    - $HOME/anaconda3/bin/conda init
    - conda env create --file /home/ubuntu/anyscale-academy/environment.yml || true
    - conda activate anyscale-academy
    - echo '. /home/ubuntu/anaconda3/etc/profile.d/conda.sh' >> ~/.bashrc
    - echo "conda deactivate; conda activate anyscale-academy" >> ~/.bashrc
    - conda install -y nodejs
    - pip list | grep jupyter
    - pip install -U jupyterlab
    - pip install -U jupyter-server-proxy
    - pip install -U notebook
    - pip list | grep jupyter
    - jupyter labextension install "@pyviz/jupyterlab_pyviz"
    - jupyter labextension update --all
    - jupyter lab build
    - jupyter labextension list

